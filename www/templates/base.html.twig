<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}My Symfony Site{% endblock %}</title>
        <link rel="stylesheet" href="{{ asset('styles/app.css') }}">
        <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
    </head>
    <body>
        <div class="background-gradient"></div>

        <nav class="navbar">
            <div class="nav-left">
                <img class="logo" src="{{ asset('images/logo-docker.png') }}" alt="logo">
                <span class="site-title">MySymfony</span>
            </div>

            <ul class="nav-links">
                <li><a href="{{ path('app_home') }}">Home</a></li>
                <li><a href="#">To-Do List</a></li>
                <li><a href="#">Add To-Do</a></li>
            </ul>

            <div class="nav-auth">
                <a href="{{ path('app_login') }}" class="nav-btn">Login</a>
                <a href="{{ path('app_register') }}" class="nav-btn register">Register</a>
            </div>
        </nav>

        <main>
            {% block body %}{% endblock %}
        </main>
    </body>
</html>
